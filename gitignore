# Python
__pycache__/
*.py[cod]
*.egg-info/
.env
venv/
.venv/

# Výstupné súbory — commituje Actions automaticky
# output/products.csv   ← v gite
# output/images/        ← v gite (deduplikované podľa názvu)
# output/scraper.log    ← v gite

# CDX cache — veľký súbor, uchovávame len cez Actions cache (nie git)
output/cdx_urls.json
output/cdx_urls.json.progress

# OS
.DS_Store
Thumbs.db
